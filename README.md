### Hey there. This is my portfolio site:

A single Go binary (`net/http` + `html/template`) that renders my portfolio
server-side and serves its own static assets. No frontend framework, no
build step.

